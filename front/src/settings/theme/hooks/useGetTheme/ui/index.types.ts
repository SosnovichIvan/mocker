export type ProjectTheme = {
  colors: {
    background: {
      dark: string
      blue: string
    }
    text: {
      white: string
      gray: string
      darkGray: string
    }
    border: {
      darkGray: string
    }
  }
}
